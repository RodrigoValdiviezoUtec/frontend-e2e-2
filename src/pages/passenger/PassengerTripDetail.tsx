import Placeholder from '../../components/Placeholder';

// TODO Persona 2 — Pantalla 4: Detalle viaje pasajero
// (GET /trips/{id} con polling 3-5s, POST /trips/{id}/rate).
export default function PassengerTripDetail() {
  return <Placeholder title="Detalle de viaje (pasajero)" owner="Persona 2" />;
}
