import Placeholder from '../../components/Placeholder';

// TODO Persona 3 — Pantalla 6: Detalle viaje conductor
// (GET /trips/{id}, PATCH /trips/{id}/complete).
export default function DriverTripDetail() {
  return <Placeholder title="Detalle de viaje (conductor)" owner="Persona 3" />;
}
